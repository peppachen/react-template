### 用于在函数组件中处理副作用

```tsx
/*
*	[]：只在初次渲染时运行。
*	[deps]：在依赖项变化时运行。
*	不传递依赖：每次组件渲染时都会运行。
* 返回值：函数，用于清理副作用。
*/
useEffect(() => {
  // 执行副作用逻辑
  return () => {
    // 清理副作用逻辑
  };
}, [dependencies]); // 依赖项数组
```
